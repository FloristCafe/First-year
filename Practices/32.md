**第二题**一个经典的构造：欧拉线性筛，多用在素数上，需要记下
**第三题**要看清楚要求：这里只需要diff的最小值，因此不需要构造node进行排序，让代码更简约更方便改正
**第四题**非常畸形的矩阵乘法(>_<)，单点修改（线段树）的操作方式过多（有撤销操作），应当用上del，cnt记录tree中的矩阵乘积，发生撤销时只需要寻找上一状态的存储节点。---如果没有撤销，直接用tree记录段乘积，再矩阵求逆进行更改是可行的
但是答案这个想法是很巧妙的：i+1回合发生状态撤销，则dp[i+1]=dp[i-1]
