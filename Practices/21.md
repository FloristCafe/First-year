**第四题**发现n<=100，敏锐察觉到可以多重dfs
这道题完全不具有子树到节点的继承关系，不应该用树思考
1.对每个节点有dp[u][k]得到最小的送k号食材的时间
下一步很容易错想希望得到最佳m元节点组合，以得到最小时间--这意味着时间复杂度达到O(n^m)，所以优化思路--不得到所有组合，只围绕单一变量T来思考
2.check(T)二分筛选最小时间，得到T以下时间的节点（n*k），看是否能找到m个达到k个食材的要求（并集）